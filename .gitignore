# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Env / secrets (por si añades backend)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Dependencies (si en el futuro usas npm)
node_modules/

# Datos del servidor / usuario (no subir para no sobrescribir lo que se guarda en la web)
server/data/*.json
server/data/
**/server/data/*.json
